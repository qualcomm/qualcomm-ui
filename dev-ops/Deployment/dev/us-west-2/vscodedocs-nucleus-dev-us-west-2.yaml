# deployment yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-vscode
  labels:
    app: react-vscode
  namespace: qui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: react-vscode
  template:
    metadata:
      labels:
        app: react-vscode
    spec:
      containers:
      - name: react-vscode
        #args: ["--environment", "dev"]
        imagePullPolicy: Always
        image: 391229415789.dkr.ecr.us-west-2.amazonaws.com/react-vscode
        ports:
          - containerPort: 80
            protocol: TCP
      nodeSelector:
        kubernetes.io/os: linux
