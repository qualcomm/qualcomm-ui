{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": [
        "*.json",
        "src/**"
      ],
      "outputs": ["dist/**"]
    },
    "test:angular:ci": {
      "inputs": [
        "*.json",
        "src/**",
        "vitest.config.ts"
      ]
    }
  }
}
