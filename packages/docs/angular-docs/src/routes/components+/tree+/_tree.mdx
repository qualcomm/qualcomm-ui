---
title: Tree
description: Tree components display hierarchical data in a collapsible structure, allowing users to explore nested relationships while keeping the interface organized and navigable.
component: Tree
aliases: [treeview, file browser, hierarchical, nested]
---

import {ComponentExplorer, QdsDemo} from "~components/demo"
import {
  TypeDocProps,
  TypeDocAngularAttributes,
} from "@qualcomm-ui/react-mdx/typedoc"

# {frontmatter.title}

{frontmatter.description}

```ts
import {TreeModule} from "@qualcomm-ui/angular/tree"
```

## Overview

- The tree relies on the `TreeCollection` class to manage its items. Refer to the [API](./#tree-collection) below for details.
- Trees are composed of nodes, which are objects that describe the tree data. There are two types of nodes:
  - A `branch` node is a node that has children.
  - A `leaf` node is a node that does not have children.
- Each node has a `value` (unique identifier used for selection/expansion) and a `text` (display text).

**Default object shape:**

- `value` (required): unique identifier for expansion/selection
- `text` (required): display text
- `nodes` (optional): child nodes for branches
- `disabled` (optional): prevents interaction when `true`

These defaults can be overridden in the `TreeCollection` [constructor](./#constructor).

## Examples

### Node Shorthand

::: terms
file explorer, folder tree, directory view, directory structure, folder hierarchy
:::

We expose the `q-tree-nodes` component for rendering Branch and Leaf nodes. Use the `<ng-template q-tree-branch-template>` and `<ng-template q-tree-leaf-template>` directives to customize the content of each tree item.

Note that `q-tree-nodes` automatically renders child nodes for branches, so you only have to customize the content of the node itself.

<QdsDemo name="TreeNodeShorthandDemo" />

> [!tip/Node Types]
> Pass the `rootNode` input to the template directives to enable TypeScript type inference. This allows `branch.node` and `leaf.node` to have your custom node type instead of the generic `TreeNode` type.

### Nodes

::: terms
recursive rendering, branch node, leaf node
:::

You can bring your own recursive component to create your own abstraction for the tree.

> [!note]
> This approach is recommended only for advanced use cases. Most users should use the shorthand `q-tree-nodes` instead.

<QdsDemo name="TreeNodesDemo" />

### Default Expanded

::: terms
initially open, open by default, expandable, collapsible, signal, two-way binding
:::

Expand nodes by default using the [defaultExpandedValue](./#defaultExpandedValue) input. Or use [expandedValue](./#expandedValue) and [expandedValueChanged](./#expandedValueChanged) to control the expansion manually. These inputs follow our [controlled state](/patterns/controlled-state) pattern.

<QdsDemo name="TreeDefaultExpandedDemo" />

### Checkbox Trees

::: terms
multi-select, selectable
:::

Use the `q-tree-node-checkbox` directive within each node to create a checkbox tree. The checked state of the tree can be controlled using the [checkedValue](./#checkedValue), [checkedValueChanged](./#checkedValueChanged), and [defaultCheckedValue](./#defaultCheckedValue) inputs, which follow our [controlled state](/patterns/controlled-state) pattern.

<QdsDemo name="TreeCheckboxDemo" />

### Checkbox selection state

::: terms
cascading, parent-child
:::

The Tree handles nested checkbox selection automatically:

- If all of a node's children are checked, the node will also be checked.
- If only some of a node's children are checked, the node will appear indeterminate to indicate partial selection.

When you supply the [checkedValue](./#checkedValue) or [defaultCheckedValue](./#defaultCheckedValue) inputs, you must account for the above logic.

::: spoiler Example

Consider the following tree:

```ts
const collection = createTreeCollection<Node>({
  rootNode: {
    id: "ROOT",
    name: "",
    nodes: [
      {
        id: "qualcomm",
        name: "Qualcomm",
        nodes: [
          {
            id: "sdx",
            name: "Snapdragon X",
            nodes: [
              {id: "elite", name: "Snapdragon X Elite"},
              {id: "plus", name: "Snapdragon X Plus"},
            ],
          },
        ],
      },
    ],
  },
})
```

Let's say that we want to select the `sdx` node and its children by default.

```ts
@Component({
  template: `
    <!-- won't work, we need to supply the child nodes instead -->
    <div q-tree-root [defaultCheckedValue]="['sdx']" [collection]="collection">
      ...
    </div>
  `,
})
export class MyComponent {
  // ...
}
```

Instead, supply all the child nodes to indicate that the parent is selected:

<QdsDemo name="TreeValidStateDemo" />

:::

The following demo shows checked state below the tree. Interact with it to see state updates:

<QdsDemo name="TreeCheckboxStatePreviewDemo" />

### Disabled Nodes

::: terms
inactive, non-interactive
:::

You can disable nodes by setting the `disabled` property on the node object.

<QdsDemo name="TreeDisabledNodeDemo" />

### Filtering

::: terms
searchable, find
:::

Here's an example that filters the nodes using `matchSorter`.

<QdsDemo name="TreeFilteringDemo" />

### Links

::: terms
navigation, router, anchor
:::

Tree nodes can be links using Angular's `RouterLink` directive. Apply the `q-tree-branch-node` or `q-tree-leaf-node` directive to an anchor element with `[routerLink]`.

<QdsDemo name="TreeLinksDemo" />

### Sizes

::: terms
compact, density, small, medium
:::

Tree item sizes are controlled using the [size](./#size) input on the root of the tree.

<QdsDemo name="TreeSizeDemo" />

### Add / Remove nodes

::: terms
dynamic, editable, CRUD
:::

The `TreeCollection` class exposes methods to handle the addition and removal of nodes. Here's an example of how to use them.

<QdsDemo name="TreeAddRemoveDemo" />

## Explorer

<ComponentExplorer
  linkPrefix="q-tree"
  name="TreeExplorerDemo"
  parts={[
    "root",
    "branch",
    "branch-node",
    "branch-trigger",
    "branch-content",
    "branch-indent-guide",
    "leaf-node",
    "node-checkbox",
    "node-icon",
    "node-indicator",
    "node-text",
  ]}
  scope="tree"
/>

## API

### q-tree-root

<TypeDocProps name="TreeRootDirective" />

<TypeDocAngularAttributes
  name={["TreeRootBindings", "QdsTreeRootBindings"]}
/>

### q-tree-nodes

<TypeDocProps name="TreeNodesComponent" />

### TreeCollection

Note that the TreeCollection accepts a single generic type parameter, `T`, which is the object type of the node used in the collection.

#### Constructor

The constructor of the TreeCollection class accepts the following options:

<TypeDocProps name="TreeCollectionOptions" partial />

<TypeDocProps hideDefaultColumn name="TreeCollection" partial />

## Element API

### q-tree-label

<TypeDocProps name="TreeLabelDirective" />

<TypeDocAngularAttributes
  name={["TreeLabelBindings", "QdsTreeLabelBindings"]}
/>

### q-tree-branch

<TypeDocProps name="TreeBranchDirective" />

<TypeDocAngularAttributes
  name={["TreeBranchBindings", "QdsTreeBranchBindings"]}
/>

### q-tree-branch-node

<TypeDocProps name="TreeBranchNodeDirective" />

<TypeDocAngularAttributes
  name={["TreeBranchNodeBindings", "QdsTreeBranchNodeBindings"]}
/>

### q-tree-branch-trigger

<TypeDocProps name="TreeBranchTriggerDirective" />

<TypeDocAngularAttributes
  name={["TreeBranchTriggerBindings", "QdsTreeBranchTriggerBindings"]}
/>

### q-tree-branch-content

<TypeDocProps name="TreeBranchContentDirective" />

<TypeDocAngularAttributes
  name={["TreeBranchContentBindings", "QdsTreeBranchContentBindings"]}
/>

### q-tree-branch-indent-guide

<TypeDocProps name="TreeBranchIndentGuideDirective" />

<TypeDocAngularAttributes name="TreeBranchIndentGuideBindings" />

### q-tree-leaf-node

<TypeDocProps name="TreeLeafNodeDirective" />

<TypeDocAngularAttributes
  name={["TreeLeafNodeBindings", "QdsTreeLeafNodeBindings"]}
/>

### q-tree-node-checkbox

<TypeDocProps name="TreeNodeCheckboxDirective" />

<TypeDocAngularAttributes
  name={["TreeNodeCheckboxBindings", "QdsCheckmarkRootBindings"]}
/>

### q-tree-node-action

<TypeDocProps name="TreeNodeActionDirective" />

<TypeDocAngularAttributes
  name={["TreeNodeActionBindings", "QdsTreeNodeActionBindings"]}
/>

### q-tree-node-indicator

<TypeDocProps name="TreeNodeIndicatorDirective" />

<TypeDocAngularAttributes
  name={["TreeNodeIndicatorBindings", "QdsTreeNodeIndicatorBindings"]}
/>

### q-tree-node-icon

<TypeDocProps name="TreeNodeIconDirective" />

<TypeDocAngularAttributes
  name={["TreeNodeIconBindings", "QdsTreeNodeIconBindings"]}
/>

### q-tree-node-text

<TypeDocProps name="TreeNodeTextDirective" />

<TypeDocAngularAttributes
  name={["TreeNodeTextBindings", "QdsTreeNodeTextBindings"]}
/>

## Template Directives

### q-tree-branch-template

<TypeDocProps name="TreeBranchTemplateDirective" />

### q-tree-leaf-template

<TypeDocProps name="TreeLeafTemplateDirective" />

Use this directive on an `ng-template` to customize the rendering of leaf nodes within `q-tree-nodes`.
