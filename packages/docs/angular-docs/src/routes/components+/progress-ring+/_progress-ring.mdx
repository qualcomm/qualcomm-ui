---
title: Progress Ring
group: Feedback
---

import {QdsDemo} from "~components/demo"
import {TypeDocProps, TypeDocAngularAttributes} from "@qualcomm-ui/mdx-docs/typedoc"

A small visual indicator that communicates ongoing task execution.

# {frontmatter.title}

```ts
import {ProgressRingModule} from "@qualcomm-ui/angular/progress-ring"
```

## Overview

The progress ring can be in one of two states:

- indeterminate: the default state.
- determinate: assumed when the [value](./#value) prop is passed.

Indeterminate progress rings keep users informed that work is happening when timeframes are unknown.

## Examples

### Simple

The simple API bundles all subcomponents together into a single directive.

<QdsDemo name="ProgressRingSimpleDemo" />

### Child Directives

Provide child directives to customize specific elements while keeping the simple API's default structure.

<QdsDemo name="ProgressRingChildDirectivesDemo" />

### Composite

Build with the composite API for granular control. This API requires you to provide each subcomponent, but gives you full control over the structure.

<QdsDemo name="ProgressRingCompositeDemo" />

### Controlled Value

When the [value](./#value) prop is set, the filled portion of the circle will represent the total percent relative to [max](./#max) and [min](./#min). This is referred to as the "determinate" state.

<QdsDemo name="ProgressRingValueDemo" />

### Value Text

Display the value text using the `q-progress-ring-value-text` directive. The value text only shows when the size is `lg` or `xl`.

<QdsDemo name="ProgressRingValueTextDemo" />

### Sizes

Use the [size](./#size) prop to change the size of the circle.

<QdsDemo name="ProgressRingSizesDemo" />

### Thickness

Use the [thickness](./#thickness) prop to change the pixel width of the track and bar.

<QdsDemo name="ProgressRingThicknessDemo" />

## Accessibility

- When the value is supplied, the component automatically provides the corresponding `aria-value*` attributes to the element.
- The progress element is automatically marked with `role="progressbar"`.
- The [label](./#label) is automatically associated with the progress ring. If you omit the [label](./#label) property, the `aria-label` of the progress element defaults to the [value](./#value) when determinate, and "Loading" when indeterminate.

## API

### q-progress-ring

The `q-progress-ring` extends the [q-progress-ring-root](./#progress-ring-root) with the following props:

<TypeDocProps
  name="ProgressRingDirective"
  omitFrom="ProgressRingRootDirective"
/>

### ProgressApi

The `ProgressApi` is accessible from the template via the `progressRingContext` structural directive:

```angular2html
<ng-container *progressRingContext="let context">
  {{ context.valuePercent }}%
</ng-container>
```

<TypeDocProps name="ProgressApi" partial />

## Composite API

### q-progress-ring-root

<TypeDocProps name="ProgressRingRootDirective" />

<TypeDocAngularAttributes
  name={["ProgressRingRootBindings", "QdsProgressRingRootBindings"]}
/>

### q-progress-ring-label

<TypeDocProps name="ProgressRingLabelDirective" />

<TypeDocAngularAttributes
  name={["ProgressLabelBindings", "QdsProgressRingLabelBindings"]}
/>

### q-progress-ring-value-text

<TypeDocProps name="ProgressRingValueTextDirective" />

<TypeDocAngularAttributes
  name={["ProgressValueTextBindings", "QdsProgressRingValueTextBindings"]}
/>

### q-progress-ring-error-text

<TypeDocProps name="ProgressRingErrorTextDirective" />

<TypeDocAngularAttributes
  name={["ProgressErrorTextBindings", "QdsProgressRingErrorTextBindings"]}
/>

### q-progress-ring-circle-container

<TypeDocProps name="ProgressRingCircleContainerDirective" />

<TypeDocAngularAttributes name="QdsProgressRingCircleContainerBindings" />

### q-progress-ring-circle

<TypeDocProps name="ProgressRingCircleDirective" />

<TypeDocAngularAttributes
  name={["ProgressRingCircleBindings", "QdsProgressRingCircleBindings"]}
/>

### q-progress-ring-track

<TypeDocProps name="ProgressRingTrackDirective" />

<TypeDocAngularAttributes
  name={["ProgressRingTrackBindings", "QdsProgressRingTrackBindings"]}
/>

### q-progress-ring-bar

<TypeDocProps name="ProgressRingBarDirective" />

<TypeDocAngularAttributes
  name={["ProgressRingBarBindings", "QdsProgressRingBarBindings"]}
/>
