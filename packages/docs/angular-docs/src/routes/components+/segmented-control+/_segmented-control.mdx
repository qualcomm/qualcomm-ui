---
title: Segmented Control
group: Data Display
---

import {
  TypeDocProps,
  TypeDocAngularAttributes,
} from "@qualcomm-ui/react-mdx/typedoc"
import {QdsDemo} from "~components/demo"

# {frontmatter.title}

```tsx
import {SegmentedControlModule} from "@qualcomm-ui/angular/segmented-control"
```

## Usage

The `SegmentedControl` component allows users to make a selection from multiple options, exclusive (similar to radio buttons) or not (similar to checkboxes).
It is typically used for filtering content, sorting elements or switching between views within the same context.

## Examples

### Simple

Use the simple API to create segmented control items using minimal markup.

<QdsDemo name="SegmentedControlSimpleDemo" />

### Composite

Build with the composite API for granular control. This API requires you to provide each subcomponent, but gives you full control over the structure and layout.

<QdsDemo name="SegmentedControlCompositeDemo" />

### Multiple

By default, a segmented control allows for a single selection. Set [multiple](./#multiple-1) to create a segmented control that allows for multiple selections.

<QdsDemo name="SegmentedControlMultipleDemo" />

### Orientation

Use [orientation](./#orientation-1) to control the layout direction of the `SegmentedControl`. Options include:

- `horizontal`: Items are laid out in a row (default).
- `vertical`: Items are laid out in a column.

<QdsDemo name="SegmentedControlOrientationDemo" />

### Icon

Use [icon](./#icon-1) on your `q-segmented-control-item` element to add icons to the segmented control items.

<QdsDemo name="SegmentedControlIconDemo" />

### Layout

Use [layout](./#layout-1) to specify how the segmented control is sized and aligned within its container. Options include:

- `hug`: Content-sized; width matches the items only (default).
- `fill`: Full width; items share space evenly.

<QdsDemo name="SegmentedControlLayoutDemo" />

### Size

Use [size](./#size-1) to control the size of the segmented control. The available sizes are `sm`, `md` (default), and `lg`.

<QdsDemo name="SegmentedControlSizeDemo" />

### Variant

Use [variant](./#variant-1) to control the visual style of the segmented control. The available variants are `primary` (default) and `neutral`.

<QdsDemo name="SegmentedControlVariantDemo" />

### Disabled

Use [disabled](./#disabled-1) on your `q-segmented-control` element to set the disabled state of the entire control.
Use [disabled](./#disabled-2) on your `q-segmented-control-item` elements to set the disabled state of individual items.

<QdsDemo name="SegmentedControlDisabledDemo" />

### Controlled state

Use [value](./#value-1) to control the selected value of the segmented control. This allows for more complex interactions and integration with form libraries.

<QdsDemo name="SegmentedControlControlledDemo" />

## Accessibility

### Keyboard Navigation

Users can navigate the segmented control using the keyboard. The following keys are supported:

- `Tab`/`Shift + Tab`: Move focus between items.
- `Space`/`Enter`: Select the focused item. Unselects an already selected item if [multiple](./#multiple-1) is set.

### Icon only

While it is possible to use icon-only items in the segmented control, it is important to ensure that they are easily understood and accessible.
Pick meaningful icons and use the `aria-label` attribute to describe their purpose.

<QdsDemo name="SegmentedControlIconOnlyDemo" />

## API

### q-segmented-control

<TypeDocProps name="SegmentedControlDirective" />

<TypeDocAngularAttributes
  name={["QdsSegmentedControlGroupBindings", "SegmentedControlGroupBindings"]}
/>

### q-segmented-control-item

The `q-segmented-control-item` component extends the [q-segmented-control-item-root](./#q-segmented-control-item-root) directive with the following attributes:

<TypeDocProps
  name="SegmentedControlItemComponent"
  omitFrom="SegmentedControlItemRootDirective"
/>

## Composite API

### q-segmented-control-item-root

<TypeDocProps name="SegmentedControlItemRootDirective" />

<TypeDocAngularAttributes
  name={[
    "QdsSegmentedControlItemBindings",
    "SegmentedControlItemBindings",
    "CheckboxRootBindings",
  ]}
/>

### q-segmented-control-item-text

<TypeDocProps name="SegmentedControlItemTextDirective" />

<TypeDocAngularAttributes
  name={["QdsSegmentedControlItemTextBindings", "CheckboxLabelBindings"]}
/>

### q-segmented-control-hidden-input

<TypeDocProps name="SegmentedControlHiddenInputDirective" />

<TypeDocAngularAttributes
  name={[
    "QdsSegmentedControlItemHiddenInputBindings",
    "CheckboxHiddenInputBindings",
  ]}
/>
