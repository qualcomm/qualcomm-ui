---
title: Icon Badge
description: The Icon Badge component combines color and iconography to visually communicate status, category, or action. It helps users quickly identify meaning through familiar symbols and consistent color cues.
component: IconBadge
aliases: [symbol badge, indicator, status icon]
badges:
  - id: since
    label: 1.4.0
    url: https://github.com/qualcomm/qualcomm-ui/blob/main/packages/frameworks/angular/CHANGELOG.md#140
---

import {QdsDemo} from "~components/demo"
import {
  TypeDocProps,
  TypeDocAngularAttributes,
} from "@qualcomm-ui/react-mdx/typedoc"

# {frontmatter.title}

{frontmatter.description}

```typescript
import {IconBadgeDirective} from "@qualcomm-ui/angular/badge"
```

## Examples

### Icon

Use the [icon](./#icon-1) input to provide a Lucide icon name, or use content projection for custom SVG content.

<QdsDemo name="IconBadgeIconDemo" />

### Emphasis

::: terms
neutral
brand
info
success
warning
danger
:::

Use the [emphasis](./#emphasis-1) input to convey semantic meaning or add color.

<QdsDemo name="IconBadgeEmphasisDemo" />

### Variant

The [variant](./#variant-1) input controls the aspect of the badge. Two variants are available: `default` (bolder appearance) and `subtle` (muted appearance).

<QdsDemo name="IconBadgeVariantDemo" />

### Size

::: terms
extra extra small
extra small
small
medium
large
extra large
:::

Control dimensions using the [size](./#size-1) input. Six sizes are available: `xxs`, `xs`, `sm`, `md` (default), `lg`, and `xl`.

<QdsDemo name="IconBadgeSizeDemo" />

### Disabled

::: terms
grayed out
:::

Set the [disabled](./#disabled-1) input to visually indicate that the badge is inactive.

<QdsDemo name="IconBadgeDisabledDemo" />

## API

### q-icon-badge

<TypeDocProps name="IconBadgeDirective" />

<TypeDocAngularAttributes name="QdsIconBadgeRootBindings" />
