---
title: Tooltip
description: Tooltips appear on demand to explain UI elements, clarify functionality, or provide helpful context without permanently occupying screen space. They automatically position themselves relative to their trigger element.
component: Tooltip
aliases: [info tip, hover text, help text, hint, popover]
---

import {
  TypeDocProps,
  TypeDocAngularAttributes,
} from "@qualcomm-ui/react-mdx/typedoc"
import {QdsDemo} from "~components/demo"

# {frontmatter.title}

{frontmatter.description}

```typescript
import {TooltipModule} from "@qualcomm-ui/angular/tooltip"
```

## Overview

::: terms
contextual help
:::

The tooltip should be used to provide additional non-essential information. It is linked to an interactive element (the trigger) and will be shown when this element is focused or hovered.
Because the tooltip itself can't be focused or tabbed to, it should not contain interactive elements.
Only one tooltip can be open at a time.

In general, tooltips should be used sparingly and only contain succinct information. Here are guidelines to help you decide when to use one:

- When using controls that lack visual labels, like icon buttons.
- When defining a term or inline item.
- When additional information may help a user make decisions.
- When providing more context to an element.

## Examples

### Simple

::: terms
basic
quick start
:::

The simple API provides a standalone component with built-in layout. The trigger is supplied as a child directive, and the content of the tooltip is the children.

<QdsDemo name="TooltipSimpleDemo" />

### Composite

::: terms
compound
parts
anatomy
subcomponents
:::

Build with the composite API for granular control. This API requires you to provide each subcomponent, but gives you full control over the structure and layout.

<QdsDemo name="TooltipCompositeDemo" />

### Placement

::: terms
alignment
left
right
bottom
start
end
:::

The tooltip's position, relative to the trigger element, can be set using the `placement` option of the [positioning](./#positioning) prop. Its default value is `top`.
Change the values in the example below and hover over the select to see the tooltip position change.

<QdsDemo name="TooltipPlacementDemo" />

### Close On Click / Escape

::: terms
dismiss
persist
:::

By default, the tooltip will close when the `Escape` key is pressed or a click is detected. You can customize this behavior using the [closeOnClick](./#closeOnClick) and [closeOnEscape](./#closeOnEscape) props.

<QdsDemo name="TooltipCloseEventsDemo" />

### Controlled Visibility

::: terms
programmatic
show hide
toggle
manual
signal
two-way binding
:::

Use [open](./#open) and [openChanged](./#openChanged) to control the component's visibility manually. These props follow our [controlled state](/patterns/controlled-state) pattern.

<QdsDemo expanded name="TooltipControlledStateDemo" />

### Disabled

::: terms
inactive
prevent
turn off
:::

You can disable the tooltip by using the `disabled` prop.

<QdsDemo name="TooltipDisabledDemo" />

## Shortcuts

### \<q-tooltip-floating-portal\>

::: terms
wrapper
shorthand
:::

A helper component that combines the portal, positioner, content, and arrow
components. This shortcut is equivalent to:

```angular2html
<q-portal>
  <div q-tooltip-positioner>
    <div q-tooltip-content>
      <div q-tooltip-arrow>
        <div q-tooltip-arrow-tip></div>
      </div>
      <ng-content />
    </div>
  </div>
</q-portal>
```

## API

### q-tooltip

The simple tooltip extends the [q-tooltip-root](./#q-tooltip-root) directive with the following props:

<TypeDocProps name="TooltipDirective" omitFrom="TooltipRootDirective" />

## Composite API

### q-tooltip-root

<TypeDocProps name="TooltipRootDirective" />

### q-tooltip-trigger

<TypeDocProps name="TooltipTriggerDirective" />

<TypeDocAngularAttributes name={["TooltipTriggerBindings"]} />

### q-tooltip-positioner

<TypeDocProps name="TooltipPositionerDirective" />

<TypeDocAngularAttributes
  name={["TooltipPositionerBindings", "QdsTooltipPositionerBindings"]}
/>

### q-tooltip-content

<TypeDocProps name="TooltipContentDirective" />

<TypeDocAngularAttributes
  name={["TooltipContentBindings", "QdsTooltipContentBindings"]}
/>

### q-tooltip-arrow

<TypeDocProps name="TooltipArrowDirective" />

<TypeDocAngularAttributes
  name={["TooltipArrowBindings", "QdsTooltipArrowBindings"]}
/>

### q-tooltip-arrow-tip

<TypeDocProps name="TooltipArrowTipDirective" />

<TypeDocAngularAttributes
  name={["TooltipArrowTipBindings", "QdsTooltipArrowTipBindings"]}
/>

## TooltipPositioningOptions

<TypeDocProps name="TooltipPositioningOptions" />
