---
title: Typography
---

import {FontTable} from "@qualcomm-ui/react-internal/theme"
import {themeFonts} from "@qualcomm-ui/tailwind-plugin/theme"
import {QdsDemo} from "~components/demo"

# {frontmatter.title}

The design system provides typography classes through either the Tailwind plugin or CSS variables. These target the [font](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/font) shorthand property.

Note that the classes on this page are only available if you've installed and configured the [@qualcomm-ui/tailwind-plugin](/tailwind) package.

## Example

<QdsDemo name="TypographyExampleDemo" />

> [!tip/Uncolored by default]
> Typography classes are not colored by default, which enables you to use them in any color context. For optimal results, pair them with a color class from our [color palette](/theming/colors).

## Static Fonts

Static fonts maintain consistent sizing across all screen sizes.

### Display

Display fonts are used for large, prominent headings and hero text.

<FontTable data={themeFonts.display} />

### Heading

Heading fonts provide a complete hierarchy for content structure.

<FontTable data={themeFonts.heading} />

### Heading Bold

Bold variants of heading fonts for emphasis.

<FontTable data={themeFonts.headingBold} />

### Body

Body fonts are used for standard text content.

<FontTable data={themeFonts.body} />

### Body Bold

Bold variants of body fonts for emphasis within content.

<FontTable data={themeFonts.bodyBold} />

### Eyebrow

Eyebrow fonts introduce or categorize a section of content.

<FontTable data={themeFonts.eyebrow} />

### Eyebrow Bold

Bold variants of eyebrow fonts for emphasis.

<FontTable data={themeFonts.eyebrowBold} />

### Code

Monospace fonts for code snippets and technical content.

<FontTable data={themeFonts.code} />

### Code Bold

Bold monospace fonts for emphasized code.

<FontTable data={themeFonts.codeBold} />

## Dynamic Fonts

Dynamic fonts scale responsively based on viewport size, providing fluid typography that adapts to different screen dimensions.

### Dynamic Display

<FontTable data={themeFonts.dynamicDisplay} />

### Dynamic Heading

<FontTable data={themeFonts.dynamicHeading} />

### Dynamic Heading Bold

<FontTable data={themeFonts.dynamicHeadingBold} />
