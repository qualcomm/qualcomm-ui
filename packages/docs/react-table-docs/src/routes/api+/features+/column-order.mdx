---
title: Column Order
---

import {TypeDocProps} from "@qualcomm-ui/react-mdx/typedoc"

# {frontmatter.title}

The column ordering feature provides methods and state for managing the order of columns in the table. These properties are available on the table instance when using the column ordering feature. See the [column ordering guide](/feature-guides/column-ordering) for usage examples and configuration details.

<TypeDocProps name="ColumnOrderInstance" partial />
