---
title: Table Sizes
---

import {Demo} from "~components/demo"
import * as demos from "./demos"

# {frontmatter.title}

## Sizes

There are three [sizes](/components/q-table#size) available for the table component: `sm`, `md`, and `lg`. The default size is `md`.

<Demo name="SizesDemo" component={demos.SizesDemo} />
