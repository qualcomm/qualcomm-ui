---
title: Filters (Client-side)
---

import {QdsDemo} from "~components/demo"

# {frontmatter.title}

::: terms
search, column filter, global filter, range filter, number filter, text filter, min max, ColumnFilterAction, setFilterValue, getFilterValue, getFilteredRowModel, getPreFilteredRowModel, debounce, filtering, client-side filtering, local filtering, large dataset, performance
:::

The following example demonstrates clientside filters for a table with 100,000 rows of data.

<QdsDemo expanded name="FiltersClientSideDemo" />
