---
title: Icon
description: Icons are small visual elements, like symbols, that represent certain functions, features, or types of content within a user interface.
component: Icon
aliases: [svg, symbol, graphic]
---

import {TypeDocAttributes, TypeDocProps} from "@qualcomm-ui/react-mdx/typedoc"
import {Demo} from "~components/demo"
import * as demos from "./demos"

# {frontmatter.title}

{frontmatter.description}

```typescript
import {Icon} from "@qualcomm-ui/react/icon"
```

## Overview

::: terms
feather
feather icons
lucide
lucide-react
:::

QUI components use icons from [Lucide](https://lucide.dev/), an open-source fork of Feather Icons.

Many components like the [Button](/components/button) and [Icon Button](/components/icon-button) have built-in support for Lucide Icons.

## Examples

### Showcase

::: terms
display icon
render icon
lucide icon
:::

Pass any Lucide icon to the `icon` prop to render it within the Icon component.

<Demo name="IconShowcaseDemo" component={demos.IconShowcaseDemo} />

### Sizes

::: terms
dimensions
resize
width
height
extra small
small
medium
large
extra large
custom size
pixel size
:::

Size can be customized using the [size](./#size) input. There are five available values: `xs`, `sm`, `md`, `lg`, `xl`. Should you need more control over the rendered size, supply a number instead which will be converted to pixels.

<Demo name="IconSizesDemo" component={demos.IconSizesDemo} />

## API

### \<Icon\>

The properties of the `<svg>` element also apply.

<TypeDocProps name="IconProps" />

<TypeDocAttributes name={["QdsIconBindings"]} />
