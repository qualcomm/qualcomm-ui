---
title: Segmented Control
description: A segmented control presents a linear set of selectable segments, supporting both single- and multi-select interactions. It offers immediate visual feedback and maintains coherence across related options.
component: SegmentedControl
aliases: [toggle group, segment picker, pill selector]
---

import {TypeDocAttributes, TypeDocProps} from "@qualcomm-ui/react-mdx/typedoc"
import {Demo} from "~components/demo"
import * as demos from "./demos"

# {frontmatter.title}

{frontmatter.description}

```tsx
import {SegmentedControl} from "@qualcomm-ui/react/segmented-control"
```

## Usage

The `SegmentedControl` component allows users to make a selection from multiple options, exclusive (similar to radio buttons) or not (similar to checkboxes).
It is typically used for filtering content, sorting elements or switching between views within the same context.

## Examples

### Simple

::: terms
basic example
getting started
:::

Use the simple API to create segmented control items using minimal markup.

<Demo
  name="SegmentedControlSimpleDemo"
  component={demos.SegmentedControlSimpleDemo}
/>

### Composite

::: terms
subcomponents
custom structure
nested components
:::

Build with the composite API for granular control. This API requires you to provide each subcomponent, but gives you full control over the structure and layout.

<Demo
  name="SegmentedControlCompositeDemo"
  component={demos.SegmentedControlCompositeDemo}
/>

### Multiple

::: terms
multi-select
checkbox-like
multiple selection
:::

By default, a segmented control allows for a single selection. Use the [multiple](./#multiple-1) prop to create a segmented control that allows for multiple selections.

<Demo
  name="SegmentedControlMultipleDemo"
  component={demos.SegmentedControlMultipleDemo}
/>

### Orientation

Use the [orientation](./#orientation-1) prop to control the layout direction of the `SegmentedControl`. Options include:

- `horizontal`: Items are laid out in a row (default).
- `vertical`: Items are laid out in a column.

<Demo
  name="SegmentedControlOrientationDemo"
  component={demos.SegmentedControlOrientationDemo}
/>

### Icon

Use the [icon](./#icon-1) prop on the `SegmentedControl.Item` or `SegmentedControl.ItemRoot` components to add icons to the segmented control items.

<Demo
  name="SegmentedControlIconDemo"
  component={demos.SegmentedControlIconDemo}
/>

### Layout

Use the [layout](./#layout-1) prop to control how the `SegmentedControl` is sized and aligned within its container. Options include:

- `hug`: Content-sized; width matches the items only (default).
- `fill`: Full width; items share space evenly.

<Demo
  name="SegmentedControlLayoutDemo"
  component={demos.SegmentedControlLayoutDemo}
/>

### Size

::: terms
small
medium
large
:::

Use the [size](./#size-1) prop to control the size of the segmented control. The available sizes are `sm`, `md` (default), and `lg`.

<Demo
  name="SegmentedControlSizeDemo"
  component={demos.SegmentedControlSizeDemo}
/>

### Variant

::: terms
appearance
styling
color scheme
:::

Use the [variant](./#variant-1) prop to control the visual style of the segmented control. The available variants are `primary` (default) and `neutral`.

<Demo
  name="SegmentedControlVariantDemo"
  component={demos.SegmentedControlVariantDemo}
/>

### Disabled

Use the segmented control's [disabled](./#disabled-1) prop to control the disabled state of the entire control. Use the segmented control item's [disabled](./#disabled-2) prop to control the disabled state of individual items.

<Demo
  name="SegmentedControlDisabledDemo"
  component={demos.SegmentedControlDisabledDemo}
/>

### Controlled state

::: terms
programmatic control
:::

Use the [value](./#value-1) prop to control the selected value of the segmented control. This allows for more complex interactions and integration with form libraries.

<Demo
  name="SegmentedControlControlledDemo"
  component={demos.SegmentedControlControlledDemo}
/>

## Accessibility

### Keyboard Navigation

::: terms
keyboard shortcuts
keyboard support
focus management
:::

Users can navigate the segmented control using the keyboard. The following keys are supported:

- `Tab`/`Shift + Tab`: Move focus between items.
- `Space`/`Enter`: Select the focused item. Unselects an already selected item if [multiple](./#multiple-1) is set.

### Icon only

::: terms
screen reader
a11y
:::

While it is possible to use icon-only items in the segmented control, it is important to ensure that they are easily understood and accessible.
Pick meaningful icons and use the `aria-label` attribute to describe their purpose.

<Demo
  name="SegmentedControlIconOnlyDemo"
  component={demos.SegmentedControlIconOnlyDemo}
/>

## API

### \<SegmentedControl.Root>

<TypeDocProps name="SegmentedControlRootProps" />

<TypeDocAttributes
  name={[
    "QdsSegmentedControlGroupBindings",
    "SegmentedControlGroupBindings",
  ]}
/>

### \<SegmentedControl.Item>

The `SegmentedControl.Item` extends the [SegmentedControl.ItemRoot](./#segmented-control-item-root) with the following props:

<TypeDocProps
  name="SegmentedControlItemProps"
  omitFrom="SegmentedControlItemRootProps"
/>

## Composite API

### \<SegmentedControl.ItemRoot>

<TypeDocProps name="SegmentedControlItemRootProps" />

<TypeDocAttributes
  name={[
    "QdsSegmentedControlItemBindings",
    "SegmentedControlItemBindings",
    "CheckboxRootBindings",
  ]}
/>

### \<SegmentedControl.ItemText>

<TypeDocProps name="SegmentedControlItemTextProps" />

<TypeDocAttributes
  name={["QdsSegmentedControlItemTextBindings", "CheckboxLabelBindings"]}
/>

### \<SegmentedControl.HiddenInput>

<TypeDocProps name="SegmentedControlHiddenInputProps" />

<TypeDocAttributes
  name={[
    "QdsSegmentedControlItemHiddenInputBindings",
    "CheckboxHiddenInputBindings",
  ]}
/>
