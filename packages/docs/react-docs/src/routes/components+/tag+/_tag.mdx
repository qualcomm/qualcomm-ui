---
title: Tag
group: Data Display
description: Tags present static metadata or contextual information. They provide a fixed visual treatment, making them ideal for displaying system-defined values, attributes, or informational labels.
component: Tag
aliases: [chip, pill, token, label]
---

import {Demo} from "~components/demo"
import * as demos from "./demos"
import {TypeDocProps, TypeDocAttributes} from "@qualcomm-ui/react-mdx/typedoc"

# {frontmatter.title}

{frontmatter.description}

```tsx
import {Tag} from "@qualcomm-ui/react/tag"
```

## Examples

### Icons

::: terms
leading icon
trailing icon
prefix
suffix
:::

Icons are supplied using the [startIcon](./#startIcon) and [endIcon](./#endIcon) props.

<Demo name="TagIconsDemo" component={demos.TagIconsDemo} />

### Variants

::: terms
clickable
removable
closable
:::

Choose from three [variants](./#variant): `link`, `selectable` and `dismissable`.

<Demo name="TagVariantsDemo" component={demos.TagVariantsDemo} />

### Colors

::: terms
status indicator
:::

Control the visual emphasis with different colors via the [emphasis](./#emphasis) prop.

<Demo name="TagEmphasisDemo" component={demos.TagEmphasisDemo} />

### Sizes

::: terms
small
medium
large
:::

Choose from three [sizes](./#size): `sm`, `md` and `lg`. The default size is `md`.

<Demo name="TagSizesDemo" component={demos.TagSizesDemo} />

### States

::: terms
inactive
:::

Disable the tag by setting the [disabled](./#disabled) prop.

<Demo name="TagStatesDemo" component={demos.TagStatesDemo} />

### Shape

::: terms
pill
:::

Control the shape of the tag with the [shape](./#shape-1) prop. The default shape is `square`.

<Demo name="TagShapeDemo" component={demos.TagShapeDemo} />

## API

### \<Tag\>

<TypeDocProps name="TagProps" />

<TypeDocAttributes name="QdsTagButtonRootBindings" />

### StartIcon

<TypeDocAttributes name="QdsTagStartIconBindings" />

### EndIcon

Note that the endIcon prop is ignored when [variant](./#variant) is `dismissible`.

<TypeDocAttributes name="QdsTagEndIconBindings" />
