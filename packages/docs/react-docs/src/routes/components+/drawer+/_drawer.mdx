---
title: Drawer
description: The drawer component creates a flexible overlay panel that provides focused workspace for forms, detailed views, or supplementary information.
component: Drawer
aliases: [side panel, offcanvas, flyout, sheet, slide out panel, sliding panel]
---

import {Demo} from "~components/demo"
import * as demos from "./demos"
import {TypeDocAttributes, TypeDocProps} from "@qualcomm-ui/react-mdx/typedoc"

# {frontmatter.title}

{frontmatter.description}

```tsx
import {Drawer} from "@qualcomm-ui/react/drawer"
```

## Overview

::: terms
app drawer
:::

The drawer's API is similar to the [Dialog](/components/dialog) component.

## Examples

### Placement End

::: terms
right drawer
right-aligned drawer
slide-in
pull-out panel
:::

The drawer's default [placement](./#placement) is `end`, which maps to the right side of the screen in LTR languages and the left side in RTL languages.

<Demo name="DrawerPlacementDemo" component={demos.DrawerPlacementDemo} />

### Placement Start

::: terms
left drawer
nav drawer
navigation drawer
sidebar drawer
slide-in
pull-out panel
:::

Use [placement](./#placement) `start` to change the placement to the left side of the screen.

<Demo
  name="DrawerPlacementStartDemo"
  component={demos.DrawerPlacementStartDemo}
/>

### Controlled State

::: terms
programmatic drawer
manage drawer state
external state control
:::

Drawer visibility can be controlled using the [open](./#open), [onOpenChange](./#onOpenChange) and [defaultOpen](./#defaultOpen) props. These props follow our [controlled state](/patterns/controlled-state) pattern.

<Demo
  name="DrawerControlledStateDemo"
  component={demos.DrawerControlledStateDemo}
/>

### Custom Container

::: terms
embedded drawer
inline drawer
nested drawer
contained drawer
non-fullscreen drawer
:::

Use the `container` prop of the `Portal` component to render the drawer in a custom container. You'll need to override the positioner's default styles to position the drawer correctly.

Consider setting [closeOnInteractOutside](./#closeOnInteractOutside) to `false` to prevent the drawer from closing when interacting outside the drawer.

<Demo
  name="DrawerCustomContainerDemo"
  component={demos.DrawerCustomContainerDemo}
  expanded
/>

## Shortcuts

### \<Drawer.FloatingPortal\>

A helper component that combines the portal, positioner, and content
components. This shortcut is equivalent to:

```tsx
<Portal {...props}>
  <Drawer.Backdrop {...backdropProps} />
  <Drawer.Positioner {...positionerProps}>
    <Drawer.Content {...contentProps}>{children}</Drawer.Content>
  </Drawer.Positioner>
</Portal>
```

## API

The Drawer shares many of its props and data attributes with the [Dialog](/components/dialog) component.

### \<Drawer.Root\>

<TypeDocProps name="DrawerRootProps" />

### \<Drawer.Backdrop\>

<TypeDocProps name="DrawerBackdropProps" />

<TypeDocAttributes
  name={["QdsDialogBackdropBindings", "DialogBackdropBindings"]}
/>

### \<Drawer.Positioner\>

<TypeDocProps name="DrawerPositionerProps" />

<TypeDocAttributes
  name={["QdsDrawerPositionerBindings", "DialogPositionerBindings"]}
/>

### \<Drawer.Content\>

<TypeDocProps name="DrawerContentProps" />

<TypeDocAttributes
  name={["QdsDrawerContentBindings", "DialogContentBindings"]}
/>

### \<Drawer.Body\>

<TypeDocProps name="DrawerBodyProps" />

<TypeDocAttributes name={["QdsDialogBodyBindings", "DialogBodyBindings"]} />

### \<Drawer.Heading\>

<TypeDocProps name="DrawerHeadingProps" />

<TypeDocAttributes
  name={["QdsDialogHeadingBindings", "DialogHeadingBindings"]}
/>

### \<Drawer.IndicatorIcon\>

<TypeDocProps name="DrawerIndicatorIconProps" />

<TypeDocAttributes name={["QdsDialogIndicatorIconBindings"]} />

### \<Drawer.CloseButton\>

<TypeDocProps name="DrawerCloseButtonProps" />

<TypeDocAttributes
  name={["QdsDialogCloseButtonBindings", "DialogCloseTriggerBindings"]}
/>
