---
title: Number Input
description: The number input component provides a structured way for users to input numerical data with built-in validation and controls. It combines a text input field with stepper buttons and can include unit selection functionality for measurements, currency, or other quantifiable values.
component: NumberInput
aliases: [spinner, stepper, numeric input]
---

import {ComponentExplorer, Demo} from "~components/demo"
import * as demos from "./demos"
import {TypeDocProps, TypeDocAttributes} from "@qualcomm-ui/react-mdx/typedoc"

# {frontmatter.title}

{frontmatter.description}

```tsx
import {NumberInput} from "@qualcomm-ui/react/number-input"
```

## Examples

### Simple

The simple API bundles all subcomponents together into a single component.

<Demo
  name="NumberInputSimpleDemo"
  component={demos.NumberInputSimpleDemo}
/>

### Composite

::: terms
compound, parts
:::

The composite pattern provides full control over the component's elements by composing individual subcomponents.

<Demo
  name="NumberInputCompositeDemo"
  component={demos.NumberInputCompositeDemo}
/>

### Min and Max values

::: terms
range, limit, bounds
:::

Pass the [min](./#min) and [max](./#max) props to the root component to set the minimum and maximum values of the number input.

If the value entered is less than min or greater than max, it will be clamped to the nearest boundary on blur or enter.

<Demo
  name="NumberInputMinMaxDemo"
  component={demos.NumberInputMinMaxDemo}
/>

### Step interval

Pass the [step](./#step) prop to the root component to set the increment or decrement step interval.

<Demo name="NumberInputStepDemo" component={demos.NumberInputStepDemo} />

### States

::: terms
disabled, readonly, required
:::

The following shows how the NumberInput component appears in each interactive state.

<Demo
  name="NumberInputStatesDemo"
  component={demos.NumberInputStatesDemo}
/>

### Controlled Value

::: terms
two-way binding, external state
:::

Use the [value](./#value), [onValueChange](./#onValueChange) and [defaultValue](./#defaultValue) props to control the value of the input field. These props follow our [controlled state](/patterns/controlled-state) pattern.

<Demo
  name="NumberInputControlledDemo"
  component={demos.NumberInputControlledDemo}
/>

### Sizes

::: terms
small, medium, large
:::

Customize size using the [size](./#size) prop. The default size is `md`.

<Demo name="NumberInputSizesDemo" component={demos.NumberInputSizesDemo} />

### Unit Selector

::: terms
currency, price, measurement
:::

Add a unit selector to the number input by passing an array of [options](./#unit-option) to the [unitOptions](./#unitOptions) prop.

The selected unit follows our [controlled state](/patterns/controlled-state) pattern: use [defaultUnit](./#defaultUnit) for uncontrolled state, or [unit](./#unit) with [onUnitChange](./#onUnitChange) for controlled state.

<Demo
  name="NumberInputUnitSelectDemo"
  component={demos.NumberInputUnitSelectDemo}
/>

### Error Text and Indicator

::: terms
validation, form error
:::

Error messages are displayed using two props:

- [invalid](./#invalid)
- [errorText](./#errorText) (or the `NumberInput.ErrorText` component when using the composite API)

The error text and indicator will only render when [invalid](./#invalid) is true.

<Demo
  name="NumberInputErrorTextDemo"
  component={demos.NumberInputErrorTextDemo}
/>

## Explorer

<ComponentExplorer
  component={demos.NumberInputExplorerDemo}
  linkPrefix="number-input"
  parts={[
    "root",
    "label",
    "input-group",
    "input",
    "control",
    "increment-trigger",
    "decrement-trigger",
    "hint",
  ]}
  scope="number-input"
/>

## API

### \<NumberInput>

The `NumberInput` component supports all props from [NumberInput.Root](./#number-input-root), plus the additional props listed below.

<TypeDocProps name="NumberInputProps" omitFrom="NumberInputRootProps" />

## Composite API

### \<NumberInput.Root>

<TypeDocProps name="NumberInputRootProps" />

<TypeDocAttributes name="NumberInputRootBindings" />

### \<NumberInput.Label>

<TypeDocProps name="NumberInputLabelProps" />

<TypeDocAttributes name="NumberInputLabelBindings" />

### \<NumberInput.InputGroup>

<TypeDocProps name="NumberInputInputGroupProps" />

<TypeDocAttributes
  name={[
    "NumberInputInputGroupBindings",
    "QdsNumberInputInputGroupBindings",
  ]}
/>

### \<NumberInput.Input>

<TypeDocProps name="NumberInputInputProps" />

<TypeDocAttributes
  name={["NumberInputInputBindings", "QdsNumberInputInputBindings"]}
/>

### \<NumberInput.Control>

<TypeDocProps name="NumberInputControlProps" />

<TypeDocAttributes
  name={["NumberInputControlBindings", "QdsNumberInputControlBindings"]}
/>

### \<NumberInput.IncrementTrigger>

<TypeDocProps name="NumberInputIncrementTriggerProps" />

<TypeDocAttributes
  name={[
    "NumberInputIncrementTriggerBindings",
    "QdsNumberInputIncrementTriggerBindings",
  ]}
/>

### \<NumberInput.DecrementTrigger>

<TypeDocProps name="NumberInputDecrementTriggerProps" />

<TypeDocAttributes
  name={[
    "NumberInputDecrementTriggerBindings",
    "QdsNumberInputDecrementTriggerBindings",
  ]}
/>

### \<NumberInput.Hint>

<TypeDocProps name="NumberInputHintProps" />

<TypeDocAttributes name="NumberInputHintBindings" />

### \<NumberInput.ErrorText>

<TypeDocProps name="NumberInputErrorTextProps" />

<TypeDocAttributes name="NumberInputErrorTextBindings" />

### \<NumberInput.ErrorIndicator>

<TypeDocProps name="NumberInputErrorIndicatorProps" />

<TypeDocAttributes
  name={[
    "NumberInputErrorIndicatorBindings",
    "QdsNumberInputErrorIndicatorBindings",
  ]}
/>

### \<NumberInput.UnitSelect>

<TypeDocProps name="NumberInputUnitSelectProps" />

<TypeDocAttributes
  name={[
    "NumberInputUnitSelectBindings",
    "QdsNumberInputUnitSelectBindings",
  ]}
/>

## Data Structures

### UnitOption

Each unit option object accepts the following properties:

<TypeDocProps name="UnitOption" partial />
