---
title: Icon Badge
description: The Icon Badge component combines color and iconography to visually communicate status, category, or action. It helps users quickly identify meaning through familiar symbols and consistent color cues.
component: IconBadge
aliases: [symbol badge, indicator, status icon]
badges:
  - id: since
    label: 1.2.0
    url: https://github.com/qualcomm/qualcomm-ui/blob/main/packages/frameworks/react/CHANGELOG.md#120
---

import {TypeDocAttributes, TypeDocProps} from "@qualcomm-ui/react-mdx/typedoc"
import {Demo} from "~components/demo"
import * as demos from "./demos"

# {frontmatter.title}

{frontmatter.description}

```typescript
import {IconBadge} from "@qualcomm-ui/react/badge"
```

## Examples

### Icon

Use the [icon](./#icon-1) prop to provide a Lucide icon, or use [children](./#children) for custom SVG content.

<Demo name="IconBadgeIconDemo" component={demos.IconBadgeIconDemo} />

### Emphasis

::: terms
neutral
brand
info
success
warning
danger
:::

Use the [emphasis](./#emphasis-1) prop to convey semantic meaning or categorize content.

<Demo name="IconBadgeEmphasisDemo" component={demos.IconBadgeEmphasisDemo} />

### Variant

The [variant](./#variant-1) prop controls the aspect of the badge. Two variants are available: `default` (bolder appearance) and `subtle` (muted appearance).

<Demo name="IconBadgeVariantDemo" component={demos.IconBadgeVariantDemo} />

### Size

::: terms
extra extra small
extra small
small
medium
large
extra large
:::

Control dimensions using the [size](./#size-1) prop. Six sizes are available: `xxs`, `xs`, `sm`, `md` (default), `lg`, and `xl`.

<Demo name="IconBadgeSizeDemo" component={demos.IconBadgeSizeDemo} />

### Disabled

::: terms
grayed out
:::

Set the [disabled](./#disabled-1) prop to visually indicate that the badge is inactive.

<Demo name="IconBadgeDisabledDemo" component={demos.IconBadgeDisabledDemo} />
## API

### \<IconBadge\>

<TypeDocProps name="IconBadgeProps" />

<TypeDocAttributes name="QdsIconBadgeRootBindings" />
