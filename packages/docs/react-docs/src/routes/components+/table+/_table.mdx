---
title: Table
description: Tables display structured data in rows and columns, supporting features like sorting, filtering, pagination, and row selection for rich data grid functionality.
component: Table
aliases: [datagrid, data table, grid view]
---

import {TypeDocProps} from "@qualcomm-ui/react-mdx/typedoc"

# {frontmatter.title}

::: terms
tabular data
:::

{frontmatter.description}

```tsx
import {Table} from "@qualcomm-ui/react/table"
```

The table component is a rich data grid with tons of functionality. Its documentation is too detailed for a single page. Head over to the [table documentation site](https://react-table-next.qui.qualcomm.com) to start building.
