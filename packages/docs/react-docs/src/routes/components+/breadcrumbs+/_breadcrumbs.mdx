---
title: Breadcrumbs
group: Disclosure
description: Breadcrumbs help users understand their current location within a website or application hierarchy and provide a quick way to navigate back to parent pages.
component: Breadcrumbs
aliases: [breadcrumb trail, navigation path]
---

import {Demo} from "~components/demo"
import * as demos from "./demos"
import {TypeDocAttributes, TypeDocProps} from "@qualcomm-ui/react-mdx/typedoc"

# {frontmatter.title}

{frontmatter.description}

```tsx
import {Breadcrumbs} from "@qualcomm-ui/react/breadcrumbs"
```

## Overview

::: terms
a11y, WCAG, site hierarchy, wayfinding, location indicator
:::

- The root component should be labeled with `aria-label="Breadcrumbs"` to indicate that it's a breadcrumb navigation.
- The first breadcrumb item is always the root item, and is typically reserved for the home page or base route of your application.
- The last breadcrumb item is always the active item.
- The last breadcrumb item should always be annotated with the [`aria-current`](https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Attributes/aria-current)`="page"` attribute.

## Examples

### Links

::: terms
polymorphic
:::

By default, breadcrumb items render as `<a>`. Without an explicit `href` or `render` prop, the anchor links to the current page. Use the [href](./#href) prop to specify the URL, or the [render](./#render-2) prop to integrate with a client-side router. The following example uses React Router:

<Demo name="BreadcrumbsLinksDemo" component={demos.BreadcrumbsLinksDemo} />

### Items

::: terms
data-driven, items array, programmatic
:::

To render a collection of breadcrumbs from data, pass an [items](./#items-1) array to `<Breadcrumbs.List>`. Each item can specify a [link](./#link) element to render a custom `<Link>` component (such as React Router's) or an [href](./#href) string for plain anchor links.

<Demo name="BreadcrumbsItemsDemo" component={demos.BreadcrumbsItemsDemo} />

### Emphasis (color)

::: terms
neutral, muted, subdued
:::

Use the [emphasis](./#emphasis) prop to change the color of the breadcrumbs.

<Demo
  name="BreadcrumbsEmphasisDemo"
  component={demos.BreadcrumbsEmphasisDemo}
/>

### Sizes

::: terms
small, medium, large
:::

Three sizes are available via the [size](./#size) prop: `sm`, `md`, and `lg`. The default size is `md`.

<Demo name="BreadcrumbsSizesDemo" component={demos.BreadcrumbsSizesDemo} />

### Disabled Item

::: terms
inactive, non-clickable
:::

Use the [disabled](./#disabled) prop to disable a breadcrumb item.

<Demo
  name="BreadcrumbsDisabledDemo"
  component={demos.BreadcrumbsDisabledDemo}
/>

### Tooltip

::: terms
tooltip, hover hint, title, description
:::

To provide additional context on hover, make [`Breadcrumbs.ItemTrigger`](./#breadcrumbsitemtrigger) a [Tooltip](/components/tooltip) trigger using the composite API.

<Demo name="BreadcrumbsTooltipDemo" component={demos.BreadcrumbsTooltipDemo} />

### Overflow

::: terms
overflow, collapsed, ellipsis, truncated
:::

Use the `<Breadcrumbs.OverflowItem>` component to collapse intermediate breadcrumb items into a dropdown menu. The overflow trigger displays an ellipsis by default. Use the [icon](./#icon-4) prop to add an icon, or provide your own trigger via `<Breadcrumbs.OverflowTrigger>`.

<Demo
  name="BreadcrumbsOverflowDemo"
  component={demos.BreadcrumbsOverflowDemo}
/>

### Max items

::: terms
overflow, collapsed, ellipsis, truncated, max items, auto collapse
:::

When using the [items](./#items) prop, add [maxItems](./#maxItems) to automatically collapse intermediate items into a dropdown menu when the total count exceeds the threshold. The first and last items remain visible by default. Use [startItems](./#startItems) and [endItems](./#endItems) to control how many items stay visible at each end.

<Demo
  name="BreadcrumbsMaxItemsDemo"
  component={demos.BreadcrumbsMaxItemsDemo}
/>

### Auto overflow

::: terms
responsive, auto overflow, dynamic, resize, container width
:::

Set [maxItems](./#maxItems) to `"auto"` to automatically collapse items that don't fit within the available space. As the container shrinks or grows, the visible items adjust accordingly. This requires the [items](./#items) prop.

<Demo
  name="BreadcrumbsAutoOverflowDemo"
  component={demos.BreadcrumbsAutoOverflowDemo}
/>

## Shortcuts

### Item

The [`Breadcrumbs.Item`](./#breadcrumbsitem) component is a convenient shorthand for rendering each part of the breadcrumb item. It's equivalent to the following:

```tsx
<Breadcrumbs.ItemRoot {...props}>
  <Breadcrumbs.ItemTrigger
    aria-current={ariaCurrent}
    href={href}
    render={render}
    {...itemTriggerProps}
  >
    {icon ? <Breadcrumbs.ItemIcon icon={icon} {...itemIconProps} /> : null}
    {children}
  </Breadcrumbs.ItemTrigger>
  <Breadcrumbs.ItemSeparator icon={separator} {...separatorProps} />
</Breadcrumbs.ItemRoot>
```

### OverflowItem

The `Breadcrumbs.OverflowItem` component is a convenient shorthand that composes a menu trigger with the overflow ellipsis. It's equivalent to the following:

```tsx
<Breadcrumbs.OverflowItemRoot {...props}>
  <Menu.Root size={menuSize}>
    <Menu.Trigger>
      <Breadcrumbs.OverflowTrigger
        aria-label={ariaLabel}
        icon={icon}
        {...overflowTriggerProps}
      />
    </Menu.Trigger>
    <Portal>
      <Menu.Positioner>
        <Menu.Content>{children}</Menu.Content>
      </Menu.Positioner>
    </Portal>
  </Menu.Root>
  <Breadcrumbs.ItemSeparator icon={separator} {...separatorProps} />
</Breadcrumbs.OverflowItemRoot>
```

## API

### \<Breadcrumbs.Root\>

<TypeDocProps name="BreadcrumbsRootProps" />

<TypeDocAttributes name="QdsBreadcrumbsRootBindings" />

### \<Breadcrumbs.List\>

<TypeDocProps name="BreadcrumbsListProps" />

<TypeDocAttributes name="QdsBreadcrumbsListBindings" />

### QdsBreadcrumbItemData

<TypeDocProps name="QdsBreadcrumbItemData" />

### \<Breadcrumbs.Item\>

<TypeDocProps name="BreadcrumbsItemProps" />

<TypeDocAttributes name="QdsBreadcrumbsItemBindings" />

### \<Breadcrumbs.ItemIcon\>

<TypeDocProps name="BreadcrumbsItemIconProps" />

<TypeDocAttributes name="QdsBreadcrumbsItemIconBindings" />

### \<Breadcrumbs.ItemTrigger\>

<TypeDocProps name="BreadcrumbsItemTriggerProps" />

<TypeDocAttributes name="QdsBreadcrumbsItemTriggerBindings" />

### \<Breadcrumbs.ItemSeparator\>

<TypeDocProps name="BreadcrumbsItemSeparatorProps" />

<TypeDocAttributes name="QdsBreadcrumbsItemSeparatorBindings" />

### \<Breadcrumbs.OverflowItem\>

<TypeDocProps name="BreadcrumbsOverflowItemProps" />

### \<Breadcrumbs.OverflowTrigger\>

<TypeDocProps name="BreadcrumbsOverflowTriggerProps" />

<TypeDocAttributes name="QdsBreadcrumbsOverflowTriggerBindings" />
