import type {ReactElement} from "react"

import {Switch} from "@qualcomm-ui/react/switch"

export function SwitchExplorerDemo(): ReactElement {
  return <Switch hint="More context here" label="Enable notifications" />
}
