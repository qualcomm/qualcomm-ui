---
title: Text Area
group: Form Controls
description: The text area component provides users with a resizable input field for entering longer text content. It includes features like character counting, validation states, and optional helper text to guide users through data entry tasks.
component: TextArea
aliases:
  [
    textarea,
    multiline input,
    multi-line input,
    paragraph input,
    long text field,
    text box,
  ]
badges:
  - id: since
    label: 1.6.0
    url: https://github.com/qualcomm/qualcomm-ui/blob/main/packages/frameworks/react/CHANGELOG.md#160
---

import {Demo} from "~components/demo"
import * as demos from "./demos"
import {TypeDocProps, TypeDocAttributes} from "@qualcomm-ui/react-mdx/typedoc"

# {frontmatter.title}

{frontmatter.description}

```tsx
import {TextArea} from "@qualcomm-ui/react/text-area"
```

## Examples

### Simple

::: terms
resizable input, expandable input
:::

The simple `<TextArea>` bundles all subcomponents together into a single component.

<Demo name="TextAreaSimpleDemo" component={demos.TextAreaSimpleDemo} />

### Composite

Build with the composite API for granular control. This API requires you to provide each subcomponent, but gives you full control over the structure and layout.

<Demo name="TextAreaCompositeDemo" component={demos.TextAreaCompositeDemo} />

### Counter

Display a character count using the [counter](./#counter-1) prop or by setting a [maxLength](./#maxLength).

By default, the counter displays:

- `count/max` when `maxLength` is set
- `count` when only `counter` is used

Use [counterProps.display](./#counterProps) to customize the format with a function that receives the current count and optional max length.

<Demo name="TextAreaCounterDemo" component={demos.TextAreaCounterDemo} />

### Sizes

::: terms
small, medium, large
:::

Customize size using the [size](./#size) prop. The default size is `md`.

<Demo name="TextAreaSizesDemo" component={demos.TextAreaSizesDemo} />

### States

::: terms
disabled state, read-only state, required field, invalid state, form states
:::

The following shows how the TextArea component appears in each interactive state.

<Demo name="TextAreaStatesDemo" component={demos.TextAreaStatesDemo} />

### Controlled State

Set the initial value using the [defaultValue](./#defaultValue) prop, or use [value](./#value) and [onValueChange](./#onValueChange) to control the value manually. These props follow our [controlled state](/patterns/controlled-state) pattern.

<Demo
  expanded
  name="TextAreaControlledStateDemo"
  component={demos.TextAreaControlledStateDemo}
/>

### Error Text and Indicator

::: terms
validation error, form validation, error message display
:::

Error messages are displayed using two props:

- [invalid](./#invalid)
- [errorText](./#errorText) (or the `TextArea.ErrorText` component when using the composite API)

The error text and indicator will only render when [invalid](./#invalid) is true.

<Demo name="TextAreaErrorTextDemo" component={demos.TextAreaErrorTextDemo} />

## Forms

Choose the form library that fits your needs—we've built examples with React Hook Form and Tanstack Form to get you started.

### React Hook Form

::: terms
rhf
:::

Use [React Hook Form](https://react-hook-form.com/) to handle the input state and validation. [ArkType](https://arktype.io/) works great for schema validation if you need it.

<Demo
  expanded
  name="TextAreaReactHookFormDemo"
  component={demos.TextAreaReactHookFormDemo}
/>

### Tanstack Form

::: terms
tanstack, @tanstack/form, tanstack-form
:::

[Tanstack Form](https://tanstack.com/form/latest/docs/framework/react/guides/basic-concepts) handles validation with its built-in validators.

<Demo
  expanded
  name="TextAreaTanstackFormDemo"
  component={demos.TextAreaTanstackFormDemo}
/>

Tanstack form also supports [ArkType](https://tanstack.com/form/latest/docs/framework/react/guides/validation#validation-through-schema-libraries).

## API

### \<TextArea /\>

The `TextArea` extends the [TextArea.Root](./#text-area-root) with the following props:

<TypeDocProps name="TextAreaProps" omitFrom="TextAreaRootProps" />

## Composite API

### \<TextArea.Root\>

<TypeDocProps name="TextAreaRootProps" />

<TypeDocAttributes name={["TextAreaRootBindings", "QdsTextAreaRootBindings"]} />

### \<TextArea.Label\>

<TypeDocProps name="TextAreaLabelProps" />

<TypeDocAttributes
  name={["TextAreaLabelBindings", "QdsTextAreaLabelBindings"]}
/>

### \<TextArea.Hint\>

<TypeDocProps name="TextAreaHintProps" />

<TypeDocAttributes name={["TextAreaHintBindings", "QdsTextAreaHintBindings"]} />

### \<TextArea.Counter\>

<TypeDocProps name="TextAreaCounterProps" />

<TypeDocAttributes
  name={["TextAreaCounterBindings", "QdsTextAreaCounterBindings"]}
/>

### \<TextArea.Input\>

<TypeDocProps name="TextAreaInputProps" />

<TypeDocAttributes
  name={["TextAreaInputBindings", "QdsTextAreaInputBindings"]}
/>

### \<TextArea.ErrorText\>

<TypeDocProps name="TextAreaErrorTextProps" />

<TypeDocAttributes
  name={["TextAreaErrorTextBindings", "QdsTextAreaErrorTextBindings"]}
/>
