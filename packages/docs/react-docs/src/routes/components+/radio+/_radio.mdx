---
title: Radio
description: Radio buttons are essential form controls that present users with a clear set of options where only one selection is possible at a time.
component: Radio
aliases: [option button, radio button]
---

import {Demo} from "~components/demo"
import * as demos from "./demos"
import {TypeDocProps} from "@qualcomm-ui/react-mdx/typedoc"

# {frontmatter.title}

{frontmatter.description}

```tsx
import {Radio, RadioGroup} from "@qualcomm-ui/react/radio"
```

## Examples

### Simple

Basic radio with label using the simple API.

<Demo name="RadioSimpleDemo" component={demos.RadioSimpleDemo} />

### Composite

::: terms
compound component
parts
:::

Build with the composite API for granular control. This API requires you to provide each subcomponent, but gives you full control over the structure and layout.

<Demo name="RadioCompositeDemo" component={demos.RadioCompositeDemo} />

### Layout

::: terms
reorder
label position
custom layout
:::

Use the composite API when you need to modify the layout of the radio buttons.

<Demo
  name="RadioCompositeLayoutDemo"
  component={demos.RadioCompositeLayoutDemo}
/>

### Disabled

When [disabled](./#disabled) is true, the radio group becomes non-interactive and is typically rendered with reduced opacity to indicate its unavailable state.

<Demo name="RadioDisabledDemo" component={demos.RadioDisabledDemo} />

### Controlled State

::: terms
useState
programmatic
two-way binding
:::

Set the initial value using the [defaultValue](./#defaultValue) prop, or use [value](./#value) and [onValueChange](./#onValueChange) to control the value manually. These props follow our [controlled state](/patterns/controlled-state) pattern.

<Demo
  expanded
  name="RadioControlledDemo"
  component={demos.RadioControlledDemo}
/>

### Orientation

::: terms
horizontal
vertical
row
column
:::

Controls the layout direction of radio buttons within the group.

<Demo name="RadioOrientationDemo" component={demos.RadioOrientationDemo} />

### Sizes

::: terms
small
medium
large
:::

The radio supports three [sizes](./#size): `sm`, `md` (default), and `lg`.

<Demo name="RadioSizesDemo" component={demos.RadioSizesDemo} />

### Indented

Use the [indented](./#indented-1) prop to indent the radio items.

<Demo name="RadioIndentedDemo" component={demos.RadioIndentedDemo} />

### Hint

::: terms
helper text
description
:::

Add a hint to provide additional context for the radio group.

<Demo name="RadioGroupHintDemo" component={demos.RadioGroupHintDemo} />

## Forms

Choose the form library that fits your needs—we've built examples with React Hook Form and Tanstack Form to get you started.

### React Hook Form

::: terms
rhf
:::

Use [React Hook Form](https://react-hook-form.com/) to handle the radio state and validation. [ArkType](https://arktype.io/) works great for schema validation if you need it.

<Demo
  expanded
  name="RadioReactHookFormDemo"
  component={demos.RadioReactHookFormDemo}
/>

### Tanstack Form

[Tanstack Form](https://tanstack.com/form/latest/docs/framework/react/guides/basic-concepts) handles radio validation with its built-in validators.

<Demo
  expanded
  name="RadioTanstackFormDemo"
  component={demos.RadioTanstackFormDemo}
/>

Tanstack form also supports [ArkType](https://tanstack.com/form/latest/docs/framework/react/guides/validation#validation-through-schema-libraries).

## Guidelines

### RadioGroup

The Radio is only intended to be used as direct descendants of the `<RadioGroup.Root>` component.

```tsx
/* Won't work alone ❌ */
<Radio label="Label" value="value" />

/* Works as expected ✅ */
<RadioGroup.Root>
  <Radio label="Label" value="value" />
</RadioGroup.Root>
```

### Composite Components

The Radio's composite components are only intended to be used as direct descendants of the `<Radio.Root>` component.

```tsx
/* Won't work alone ❌ */
<Radio.HiddenInput />
<Radio.Control />

/* Works as expected ✅ */
<Radio.Root>
  <Radio.HiddenInput />
  <Radio.Control />
</Radio.Root>
```

## API

### \<RadioGroup.Root\>

<TypeDocProps name="RadioGroupRootProps" />

### \<RadioGroup.Hint\>

<TypeDocProps name="RadioGroupHintProps" />

### \<Radio.Root\>

<TypeDocProps name="RadioRootProps" />

### \<Radio\>

The `Radio` component supports all props from [Radio.Root](./#radio-root), plus the additional props listed below.

<TypeDocProps name="RadioProps" omitFrom="RadioRootProps" />
