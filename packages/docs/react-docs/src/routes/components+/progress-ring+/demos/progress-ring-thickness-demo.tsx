import type {ReactElement} from "react"

import {ProgressRing} from "@qualcomm-ui/react/progress-ring"

export function ProgressRingThicknessDemo(): ReactElement {
  return (
    // preview
    <ProgressRing className="py-2" size="lg" thickness={8} />
    // preview
  )
}
