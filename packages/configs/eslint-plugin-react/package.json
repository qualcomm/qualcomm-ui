{
  "name": "@qualcomm-ui/eslint-plugin-react",
  "version": "1.2.2",
  "description": "ESLint plugin for enforcing best-practices with QUI React component consumption",
  "keywords": [
    "a11y",
    "accessibility",
    "eslint",
    "plugin",
    "react"
  ],
  "license": "BSD-3-Clause-Clear",
  "author": "Ryan Bower",
  "repository": "https://github.com/qualcomm/qualcomm-ui",
  "files": [
    "dist/"
  ],
  "type": "module",
  "sideEffects": false,
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "shx rm -rf node_modules/.tmp dist",
    "build": "run-s clean build:js build:types",
    "build:js": "vite build",
    "build:types": "tsc -b tsconfig.lib.json",
    "build:watch": "run-p watch:js watch:types",
    "watch:js": "vite build -w",
    "watch:types": "tsc -b tsconfig.lib.json -w --preserveWatchOutput",
    "lint": "eslint --quiet",
    "test": "pnpm build && vitest -c vitest.config.ts --run",
    "test:unit:ci": "pnpm build && cross-env NODE_OPTIONS='' vitest -c vitest.config.ts --run --pool=threads --no-file-parallelism",
    "prepublishOnly": "pnpm build && qui-cli pre-publish",
    "postpublish": "qui-cli post-publish"
  },
  "devDependencies": {
    "@eslint/core": "^1.0.0",
    "@qualcomm-ui/cli": "catalog:",
    "@qualcomm-ui/tsconfig": "catalog:",
    "@qualcomm-ui/vite": "catalog:",
    "@typescript-eslint/rule-tester": "catalog:",
    "@typescript-eslint/utils": "catalog:",
    "npm-run-all2": "catalog:",
    "shx": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "typescript-eslint": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  },
  "peerDependencies": {
    "eslint": "^9.0.0"
  }
}
