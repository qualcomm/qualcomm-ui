# Useful Commands

## NCU

- Check Angular minor versions for updates:

```shell
ncu --workspaces --root --filter '@angular-devkit/build-angular' --filter '@angular-devkit/core' --filter '@angular/*' --target minor
```
